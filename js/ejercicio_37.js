// Un camión transporta 25 cajas de repuestos de carros. Si cada caja pesa 748 Kg ¿cuántos
// Kg transporta?

const cajas = 25;
const peso_cada_caja = 748;

let peso_total = cajas * peso_cada_caja;
alert(`El camion transporta un total de ${peso_total} kg por las 25 cajas`);